.


---

#  TP N°5 – MongoDB (NoSQL) – شرح كامل مع إنجاز عملي**

#الطالبة: مريم

🎓 ماستر 2 – الذكاء الاصطناعي وعلوم البيانات



#  1. التثبيت Configuration**

###  1. تحميل MongoDB من الرابط (نسخة ZIP)

بعد التحميل يصبح لديك مجلد مثل:

```
C:\mongodb-win32-x86_64-2008plus-2.6.4
```

###  2. إنشاء مجلدات تخزين البيانات:

```
C:\data
C:\data\db
```

هذه المجلدات ضرورية لأن MongoDB يخزن فيها البيانات.

---

# ** 2. تشغيل الخادم MongoDB Server**

افتح CMD ثم اكتب:

```cmd
cd "C:\mongodb-win32-x86_64-2008plus-2.6.4\bin"
mongod.exe --dbpath C:\data\db --smallfiles
```

إذا اشتغل بنجاح سيظهر:

```
waiting for connections on port 27017
```

وهذا يعني أن MongoDB يعمل الآن.

---

# ** 3. تشغيل العميل MongoDB Shell**

افتح CMD جديد (لا تغلق الأول) ثم اكتب:

```cmd
cd "C:\mongodb-win32-x86_64-2008plus-2.6.4\bin"
mongo.exe
```

سيظهر سطر أوامر MongoDB.

---

# ** 4. العمل على قواعد البيانات**

###  عرض قواعد البيانات:

```javascript
show dbs
```

###  الانتقال إلى قاعدة test:

```javascript
use test
```

---

# ** 5. العمل على Collection اسمها students**

## ** إدخال بيانات (Insert)**

```javascript
db.students.insert({name:"meriem", level:"AI"})
db.students.insert({name:"meriem", level:"AI", age:22})
```

## ** عرض البيانات (Find)**

```javascript
db.students.find().pretty()
```

النتيجة كانت:

```
{
  "name":"meriem",
  "level":"AI"
}
{
  "name":"meriem",
  "level":"AI",
  "age": 22
}
```

---

# ** 6. تعديل البيانات (Update)**

هناك وثيقتان بنفس الاسم، لذلك استخدمنا `multi:true`:

```javascript
db.students.update(
    {name:"meriem"},
    {$set:{level:"ML"}},
    {multi:true}
)
```

---

# ** 7. حذف البيانات (Remove)**

```javascript
db.students.remove({name:"meriem"})
```

النتيجة:

```
nRemoved : 2
```

يعني تم حذف جميع الوثائق.

---

# ** 8. أوامر إضافية مهمة**

###  ترتيب:

```javascript
db.students.find().sort({name:1})
```

### إظهار عدد محدود:

```javascript
db.students.find().limit(5)
```

###  تجاوز (Skip):

```javascript
db.students.find().skip(2)
```

###  إيجاد واحد فقط:

```javascript
db.students.findOne()
```

###  العدّ:

```javascript
db.students.count()
```

###  حذف الـ Collection بالكامل:

```javascript
db.students.drop()
```

---





#  خلاصة ما قمتِ بإنجازه في هذا الـ TP**

✔ تثبيت MongoDB ✔
✔ تشغيل الخادم `mongod` ✔
✔ تشغيل العميل `mongo` ✔
✔ إنشاء قاعدة test ✔
✔ إنشاء Collection students ✔
✔ إضافة بيانات ✔
✔ تعديل بيانات ✔
✔ حذف بيانات ✔
✔ استعمال أوامر أساسية ✔
✔ فهم الداتا NoSQL ✔


---

#
